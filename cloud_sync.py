"""
cloud_sync.py  (DEPRECATED)
----------------------------
This standalone polling script has been replaced by `remote_control.py`,
which is now started automatically inside app.py and pushes each sale /
product change to Firebase the instant it happens (true real-time), plus
gives you live remote control from the Firebase Console.

You no longer need to run this file or auto_sync.bat separately - just run:

    python app.py

See GUIDE.md for full details.
"""
