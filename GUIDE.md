# Firebase দিয়ে Real-Time Backup ও Remote Control — সম্পূর্ণ গাইড

এই গাইডটি আপনার আপলোড করা **Supershop_Fixed** (DOINEEK POS) অ্যাপের জন্য তৈরি।
আপনার আপলোড করা `firebase_credentials.json` ফাইলটি দেখে বোঝা গেছে এটি ইতিমধ্যে
`doineek-pos-e4023` নামের একটি Firebase প্রজেক্টের সাথে যুক্ত করা আছে — তাই নতুন
করে প্রজেক্ট বানাতে হবে না।

আপনার আগের `cloud_sync.py` ফাইলে একটি বাগ ছিল — এটি `sales` টেবিলে
`is_synced` নামের একটি কলাম খুঁজত, যা আসলে ডাটাবেজে ছিলই না। ফলে স্ক্রিপ্টটি
চালু হলেই ভেঙে যেত এবং কোনো সিঙ্ক আসলে হতোই না। আমি এটি ঠিক করে দিয়েছি এবং
পুরো সিস্টেমটি নতুন করে, সত্যিকারের real-time পদ্ধতিতে সাজিয়ে দিয়েছি।

---

## ১. এখন সিস্টেমটি কীভাবে কাজ করে (Architecture)

```
                    ┌────────────────────────┐
   POS ক্যাশিয়ার  →  │   app.py (Flask)        │
   বিক্রি করলে      │   + remote_control.py  │
                    └───────────┬────────────┘
                                │  (checkout হওয়ার সাথে সাথেই push হয়,
                                │   টাইমার বা delay নেই)
                                ▼
                    ┌────────────────────────┐
                    │   Firebase / Firestore  │  ← আপনি এখান থেকে
                    │   (Cloud Database)      │     দোকান নিয়ন্ত্রণ করেন
                    └───────────┬────────────┘
                                │  (live listener, ~১ সেকেন্ডের মধ্যে)
                                ▼
                    ┌────────────────────────┐
                    │   দোকানের app.py আবার   │
                    │   পরিবর্তনটি প্রয়োগ করে │
                    └────────────────────────┘
```

দুইটি জিনিস এখন একসাথে হচ্ছে:

1. **Real-time Backup (দোকান → ক্লাউড):** প্রতিটি বিক্রি (sale), প্রতিটি
   প্রোডাক্ট যোগ/এডিট/ডিলিট হওয়ার সাথে সাথেই — অপেক্ষা না করে — Firestore-এ
   পাঠিয়ে দেওয়া হয়। আগে এটি প্রতি ২ মিনিটে একবার হতো (এবং বাগের কারণে আসলে
   হতোই না)।

2. **Remote Control (ক্লাউড → দোকান):** Firebase Console-এ গিয়ে
   `remote_control/settings` নামের একটি ডকুমেন্ট এডিট করলেই — সেটা দোকানের
   চলমান অ্যাপে সাথে সাথে প্রয়োগ হয়ে যায়, অ্যাপ রিস্টার্ট বা নতুন কোড আপলোড
   ছাড়াই।

---

## ২. যেসব ফাইল যোগ/পরিবর্তন করা হয়েছে

| ফাইল | কী পরিবর্তন হয়েছে |
|---|---|
| `remote_control.py` | **নতুন ফাইল।** Real-time backup + remote control-এর মূল ইঞ্জিন। |
| `app.py` | `remote_control` ইম্পোর্ট করা হয়েছে, `before_request` হুক যোগ হয়েছে (maintenance mode চেক করার জন্য), এবং checkout/product routes-এ push কল যোগ হয়েছে। |
| `database.py` | `sales` টেবিলে `is_synced` কলাম যোগ করার migration যোগ হয়েছে। |
| `requirements.txt` | `firebase-admin` প্যাকেজ যোগ হয়েছে। |
| `firestore.rules` | Firestore-এর জন্য সুরক্ষা নিয়ম (security rules)। |
| `cloud_sync.py` | পুরনো, ভাঙা স্ক্রিপ্ট — এখন শুধু একটি নোট রাখা আছে, এটি আর চালাতে হবে না। |
| `auto_sync.bat` | মুছে ফেলা হয়েছে — আলাদা করে সিঙ্ক স্ক্রিপ্ট চালানোর দরকার নেই, `python app.py` চালালেই সব হয়ে যায়। |

**আপনার সাইটের ডিজাইন, টেমপ্লেট, বা কোনো ফিচারের আচরণ পরিবর্তন হয়নি** — POS,
প্রোডাক্ট ম্যানেজমেন্ট, বিক্রি, রিপোর্ট — সব হুবহু আগের মতোই কাজ করবে। শুধু
Firebase অংশটুকু যোগ/মেরামত করা হয়েছে।

---

## ৩. চালু করার ধাপ

### ধাপ ১ — প্যাকেজ ইনস্টল
```bash
cd supershop
pip install -r requirements.txt
```

### ধাপ ২ — অ্যাপ চালু করুন (আগের মতোই)
```bash
python app.py
```
টার্মিনালে দেখতে পাবেন:
```
✅ Firebase real-time backup & remote control started.
```
এর মানে অ্যাপ Firestore-এর সাথে সংযুক্ত হয়ে গেছে এবং লাইভ শোনা শুরু করেছে।

> এখন থেকে `cloud_sync.py` বা `auto_sync.bat` আলাদা করে চালানোর দরকার নেই —
> সবকিছু `app.py`-এর ভেতরেই চলবে।

---

## ৪. Firebase Console থেকে যা যা দেখতে/করতে পারবেন

Firebase Console → আপনার প্রজেক্ট (`doineek-pos-e4023`) → **Firestore
Database** এ গেলে নিচের কালেকশনগুলো দেখবেন:

- **`sales`** — প্রতিটি বিক্রির ইনভয়েস, লাইভ। যেই মুহূর্তে ক্যাশিয়ার
  checkout করবে, সাথে সাথে এখানে নতুন ডকুমেন্ট আসবে। এটিই আপনার **real-time
  ব্যাকআপ** — দোকানের কম্পিউটার নষ্ট হলেও সব বিক্রির রেকর্ড এখানে সুরক্ষিত
  থাকবে।
- **`products`** — বর্তমান স্টক, দাম, ইত্যাদির লাইভ কপি।
- **`settings`** — দোকানের নাম/ঠিকানা/ফোন/VAT নম্বরের ব্যাকআপ।
- **`remote_control/settings`** — এই একটি ডকুমেন্টই আপনার **রিমোট কন্ট্রোল
  প্যানেল**। এখানে গিয়ে ফিল্ডগুলো (নিচে দেখুন) পরিবর্তন করলেই দোকানের অ্যাপে
  সাথে সাথে প্রভাব পড়বে।

### `remote_control/settings` ডকুমেন্টের ফিল্ডগুলো

| ফিল্ড | টাইপ | কাজ |
|---|---|---|
| `maintenance_mode` | boolean | `true` করলে পুরো POS সাইট বন্ধ হয়ে "সাময়িক বন্ধ" পেজ দেখাবে (login বাদে)। `false` করলে ১৫ সেকেন্ডের মধ্যে আবার স্বাভাবিক হয়ে যাবে। |
| `maintenance_message` | string | মেইনটেনেন্স চলাকালীন কী মেসেজ দেখাবে, তা কাস্টমাইজ করুন। |
| `announcement` | string | (ঐচ্ছিক, ভবিষ্যতে UI-তে দেখানোর জন্য প্রস্তুত রাখা হয়েছে) |
| `force_logout` | boolean | `true` করলে সব ইউজারের সেশন সাথে সাথেই লগআউট হয়ে যাবে, তারা আবার লগইন করতে বাধ্য হবে। কাজ শেষে আবার `false` করে দিন। |

**উদাহরণ:** দোকান বন্ধ রেখে ইনভেন্টরি গোনার সময় POS বন্ধ রাখতে চাইলে —
Console-এ গিয়ে `maintenance_mode` = `true` করে দিন। কাজ শেষে আবার `false`
করে দিলেই সব স্বাভাবিক।

---

## ৫. প্রোডাক্ট/দাম সরাসরি Firebase Console থেকে পরিবর্তন

`products` কালেকশনে যেকোনো প্রোডাক্টের ডকুমেন্টে গিয়ে (ডকুমেন্ট ID = SKU)
`sell_price`, `stock_qty` ইত্যাদি পরিবর্তন করে সরাসরি ক্লাউড থেকেও ডেটা
আপডেট করা সম্ভব — তবে **বর্তমান কোডে এই পরিবর্তন local SQLite ডাটাবেজে
ফিরিয়ে আনার (download) কোনো listener যোগ করা হয়নি**, কারণ এটি একটি
স্পর্শকাতর সিদ্ধান্ত (দাম/স্টক ভুলভাবে ওভাররাইট হয়ে যাওয়ার ঝুঁকি থাকে)।
POS-এর মূল সত্য উৎস (source of truth) সবসময় দোকানের লোকাল অ্যাপ, ক্লাউড
শুধু ব্যাকআপ ও কন্ট্রোল প্যানেল হিসেবে কাজ করে।

আপনি যদি ভবিষ্যতে চান যে Console থেকে দাম/স্টক পরিবর্তন করলে তা সরাসরি
দোকানের ডাটাবেজেও নেমে আসুক (two-way sync), সেটি `remote_control.py`-তে
`products` কালেকশনের উপর আরেকটি `on_snapshot` listener বসিয়ে করা সম্ভব —
বললে আমি সেটাও তৈরি করে দিতে পারি।

---

## ৬. নিরাপত্তা (Security) — অবশ্যই মেনে চলুন

- **`firebase_credentials.json` কখনো শেয়ার করবেন না বা কোনো পাবলিক GitHub
  রিপোতে আপলোড করবেন না।** এই ফাইল থাকলে যে কেউ আপনার পুরো Firestore ডাটাবেজ
  নিয়ন্ত্রণ করতে পারবে। এটি শুধু দোকানের কম্পিউটারে, লোকাল ফোল্ডারে রাখুন।
- Firebase প্রজেক্টে **Project Settings → Users and permissions**-এ গিয়ে
  শুধু বিশ্বাসযোগ্য মানুষকে (যেমন আপনি নিজে ও দোকান-মালিক) Owner/Editor
  হিসেবে যোগ করুন।
- আপনার Google অ্যাকাউন্টে **2-Step Verification (MFA)** চালু রাখুন, যেহেতু
  এই অ্যাকাউন্ট দিয়েই পুরো দোকান রিমোটলি নিয়ন্ত্রণ করা যাচ্ছে।
- আমি `firestore.rules` ফাইলে ডিফল্ট হিসেবে সব পাবলিক (ব্রাউজার/মোবাইল)
  read/write বন্ধ রেখেছি — শুধু আপনার সার্ভার (Admin SDK) ও Console থেকে
  অ্যাক্সেস করা যাবে। এই rules ফাইলটি **Firebase Console → Firestore
  Database → Rules** ট্যাবে পেস্ট করে Publish করুন।

---

## ৭. ঐচ্ছিক (Advanced) — স্বয়ংক্রিয় ক্লাউড স্টোরেজ এক্সপোর্ট

Firestore নিজেই ইতিমধ্যে টেকসই, রিয়েল-টাইম ব্যাকআপ। এর উপরে অতিরিক্ত একটি
স্তর হিসেবে, চাইলে Google Cloud-এর **Scheduled Firestore Export** সেট করা
যায় — যা প্রতিদিন স্বয়ংক্রিয়ভাবে পুরো ডাটাবেজের একটি কপি Google Cloud
Storage-এ রেখে দেয়। এর জন্য প্রজেক্টকে "Blaze" (pay-as-you-go) প্ল্যানে
থাকতে হয় এবং `gcloud` CLI বা Cloud Scheduler + Cloud Function সেটআপ করতে
হয়। এটি সাধারণ ছোট দোকানের জন্য জরুরি নয় (Firestore নিজেই একাধিক
ডেটাসেন্টারে ডেটা রাখে), তবে চাইলে বলবেন, আমি এই অংশটির জন্যও step-by-step
গাইড ও স্ক্রিপ্ট বানিয়ে দিতে পারি।

---

## ৮. দ্রুত পরীক্ষা করার উপায় (Testing)

1. `python app.py` চালান এবং কনসোলে ✅ মেসেজ নিশ্চিত করুন।
2. একটি টেস্ট বিক্রি (sale) করুন POS থেকে → Firebase Console-এর `sales`
   কালেকশনে কয়েক সেকেন্ডের মধ্যেই নতুন ডকুমেন্ট দেখা উচিত।
3. Firebase Console-এ গিয়ে `remote_control/settings`-এ `maintenance_mode`
   `true` করুন → দোকানের ব্রাউজারে যেকোনো পেজ রিফ্রেশ করলেই "সাময়িক বন্ধ"
   মেসেজ দেখা উচিত (১৫ সেকেন্ডের মধ্যে auto-refresh হবে)।
4. আবার `false` করে স্বাভাবিক অবস্থায় ফিরিয়ে আনুন।

---

কোনো ধাপে আটকে গেলে, বা two-way product sync / স্বয়ংক্রিয় Cloud Storage
এক্সপোর্ট যোগ করতে চাইলে জানাবেন — আমি সেই অংশটুকু বানিয়ে দেব।
